{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "浮窗与设置窗的默认权限：核心事件监听（emit/listen）。热键、剪贴板、复制均在 Rust 侧完成，无需前端权限。",
  "windows": ["main", "settings"],
  "permissions": ["core:default"]
}
