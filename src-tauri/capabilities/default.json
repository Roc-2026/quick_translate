{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "浮窗默认权限：核心事件监听（emit/listen）。热键、剪贴板、复制均在 Rust 侧完成，无需前端权限。",
  "windows": ["main"],
  "permissions": ["core:default"]
}
